checks:
  file-lines:
    config:
      threshold: 250
  method-complexity:
    config:
      threshold: 15
  similar-code:
    enabled: true
    config:
      threshold: #language-specific defaults. overrides affect all languages.
exclude_patterns:
  - 'src/abis'
  - 'src/hooks'
